trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:

 - script: dotnet build ShiftIdentity.Model --configuration release
   displayName: 'dotnet build ShiftIdentity.Model'

 - script: dotnet pack ShiftIdentity.Model --configuration release --output $(Build.ArtifactStagingDirectory)
   displayName: 'dotnet pack'

 - task: NuGetCommand@2
   inputs:
    command: 'push'
    packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg;!$(Build.ArtifactStagingDirectory)/**/*.symbols.nupkg'
    nuGetFeedType: 'external'
    publishFeedCredentials: 'NuGet.org'