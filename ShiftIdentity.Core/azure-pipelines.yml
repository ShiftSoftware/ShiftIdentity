trigger:
- master

pool:
  vmImage: ubuntu-latest

steps:

 - script: dotnet build ShiftSoftware.ShiftIdentity.Core --configuration release
   displayName: 'dotnet build ShiftSoftware.ShiftIdentity.Core'

 - script: dotnet pack ShiftSoftware.ShiftIdentity.Core --configuration release --output $(Build.ArtifactStagingDirectory)
   displayName: 'dotnet pack'

 - task: NuGetCommand@2
   inputs:
    command: 'push'
    packagesToPush: '$(Build.ArtifactStagingDirectory)/**/*.nupkg;!$(Build.ArtifactStagingDirectory)/**/*.symbols.nupkg'
    nuGetFeedType: 'external'
    publishFeedCredentials: 'NuGet.org'